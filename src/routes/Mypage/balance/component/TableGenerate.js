import React from 'react'

function TableGenerate({transferlist,all, currentPage}){
    let all = all[0]-all[1]
    let before = a;
    let after = a;
    letcount =0;
    



}
export default React.memeo(TableGenerate);